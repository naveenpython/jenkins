pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'date'
            }
        }
        stage('Test') { 
            steps {
                sh 'ls'
            }
        }
        stage('Deploy') { 
            steps {
                sh 'echo SUCCESS'
            }
        }
    }
}
